//use this to import all of the powerups, and then export them all. this means whenever a new powerup is added, we only have to edit this file and not every file that uses powerups

import ExamplePowerup from "./examplePowerup";
import Powerup from "./powerup";

export { ExamplePowerup, Powerup };