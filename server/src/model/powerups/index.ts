//use this to  export them all. this means whenever a new powerup is added, we only have to edit this file and not every file that uses powerups

export { default as ExamplePowerup } from './examplePowerup';
